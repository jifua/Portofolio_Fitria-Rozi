# Database (SQLite)

This folder contains the structured database used for analysis.

## File

* `production.db` → SQLite database

## Description

The database stores cleaned production data and is used for SQL-based analysis.

## Usage

* Query using SQL scripts (see SQL folder)
* Used as source for analytical insights

## Purpose

* Simulates real-world database environment
* Supports structured querying for production analysis

## Why Important

Using a database reflects real industry workflow where data is stored and queried efficiently.

